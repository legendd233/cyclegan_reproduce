# CycleGAN 复现数据分析报告

## 1. 论文概述

**论文标题：** Unpaired Image-to-Image Translation using Cycle-Consistent Adversarial Networks

**作者：** Jun-Yan Zhu, Taesung Park, Phillip Isola, Alexei A. Efros (UC Berkeley BAIR)

CycleGAN 提出了一种在**无配对数据**条件下进行图像到图像翻译的方法。核心思想是同时训练两个映射函数 G: X→Y 和 F: Y→X，并通过**循环一致性损失（Cycle Consistency Loss）** 约束 F(G(x)) ≈ x 和 G(F(y)) ≈ y，从而避免模式坍缩并确保映射的语义一致性。

### 1.1 损失函数

完整目标函数由三部分组成：

$$\mathcal{L}(G, F, D_X, D_Y) = \mathcal{L}_{GAN}(G, D_Y, X, Y) + \mathcal{L}_{GAN}(F, D_X, Y, X) + \lambda \mathcal{L}_{cyc}(G, F)$$

其中：
- **对抗损失 (GAN Loss)**：采用 LSGAN（最小二乘损失）替代原始 GAN 的交叉熵损失，训练更稳定
- **循环一致性损失 (Cycle Consistency Loss)**：使用 L1 范数，权重 λ=10
- **身份损失 (Identity Loss)**：当输入图像已属于目标域时，鼓励生成器保持输入不变，权重系数为 0.5λ

### 1.2 网络架构（论文设定）

| 组件 | 架构 |
|------|------|
| 生成器 G/F | ResNet, 9 residual blocks (256×256输入) |
| 判别器 D_X/D_Y | 70×70 PatchGAN, 3层 |
| 归一化 | Instance Normalization |
| 优化器 | Adam (β1=0.5, lr=0.0002) |
| Batch Size | 1 |
| 图像缓冲池 | 50 张历史生成图像 |

### 1.3 训练策略

- 前 100 个 epoch 保持学习率 0.0002 不变
- 后 100 个 epoch 线性衰减至 0

---

## 2. 复现实验配置

本次复现在两个数据集上进行了实验：**maps**（地图↔航拍图）和 **monet2photo**（莫奈画作↔真实照片）。

### 2.1 Maps 数据集实验配置

| 参数 | 复现值 | 论文值 | 是否一致 |
|------|--------|--------|----------|
| dataroot | ./datasets/maps | maps | ✅ |
| model | cycle_gan | CycleGAN | ✅ |
| netG | resnet_9blocks | ResNet 9 blocks | ✅ |
| netD | basic (PatchGAN) | 70×70 PatchGAN | ✅ |
| ngf / ndf | 64 / 64 | 64 / 64 | ✅ |
| n_layers_D | 3 | 3 | ✅ |
| norm | instance | Instance Norm | ✅ |
| gan_mode | lsgan | LSGAN | ✅ |
| lr | 0.0002 | 0.0002 | ✅ |
| beta1 | 0.5 | 0.5 | ✅ |
| batch_size | 1 | 1 | ✅ |
| lambda_A / lambda_B | 10.0 / 10.0 | λ=10 | ✅ |
| lambda_identity | 0.5 | 0.5 | ✅ |
| pool_size | 50 | 50 | ✅ |
| n_epochs | 100 | 100 | ✅ |
| n_epochs_decay | 100 | 100 | ✅ |
| load_size / crop_size | 286 / 256 | 286 / 256 | ✅ |
| preprocess | resize_and_crop | resize_and_crop | ✅ |
| no_dropout | True | True | ✅ |

**结论：Maps 实验的所有超参数与论文完全一致。**

### 2.2 Monet2Photo 数据集实验配置

| 参数 | 复现值 | 论文值 | 是否一致 |
|------|--------|--------|----------|
| dataroot | ./datasets/monet2photo | monet2photo | ✅ |
| 其余参数 | 同 Maps 实验 | 同上 | ✅ |
| continue_train | True (从 epoch 108 续训) | — | 注意 |

**注意事项：** Monet 的 `loss_log.txt` 包含三段 session（由 "Training Loss" header 分隔）：

| Session | 时间戳 | 内容 | 处理 |
|---------|--------|------|------|
| 1 | Wed Mar 11 02:07 | 首次训练 epoch 1-108, 6767 条 | 保留 |
| 2 | Fri Mar 13 01:42 | 误启动 epoch 1, 仅 21 条 | **丢弃** |
| 3 | Fri Mar 13 01:47 | 续训 epoch 108-200, 5846 条 | 保留 (去重 epoch 108) |

数据分析脚本 `plot_losses.py` 在解析时自动识别并丢弃了 Session 2 的误启动数据，并对 Session 1 和 Session 3 在 epoch 108 的重叠记录做了去重处理，最终拼接为 epoch 1-200 共 200 个 epoch、12551 条记录的连续时间线。

---

## 3. 训练过程分析

### 3.1 Maps 数据集训练损失

训练从 2026-03-10 23:43 开始，共完成 200 个 epoch。每个 epoch 约 1096 次迭代（约 1096 张训练图像）。

#### 3.1.1 损失变化趋势

| 阶段 | Epoch | cycle_A | cycle_B | D_A | D_B | G_A | G_B | idt_A | idt_B |
|------|-------|---------|---------|-----|-----|-----|-----|-------|-------|
| 初期 | 1 | 1.2~3.1 | 0.7~2.0 | 0.12~0.38 | 0.17~0.42 | 0.15~0.55 | 0.19~0.45 | 0.28~0.96 | 0.58~1.56 |
| 中期 | ~50 | 0.6~1.0 | 0.2~0.5 | 0.05~0.30 | 0.10~0.30 | 0.1~0.8 | 0.1~0.5 | 0.06~0.15 | 0.10~0.25 |
| 稳定期 | ~100 | 0.4~0.8 | 0.1~0.4 | 0.03~0.30 | 0.07~0.29 | 0.1~1.0 | 0.1~1.2 | 0.05~0.14 | 0.10~0.34 |
| 末期 | 200 | 0.4~0.8 | 0.1~0.4 | 0.03~0.20 | 0.14~0.24 | 0.3~1.1 | 0.26~0.38 | 0.04~0.11 | 0.08~0.16 |

#### 3.1.2 Cycle Consistency Loss 与 Identity Loss 曲线

![Maps — Cycle Consistency Loss 与 Identity Loss](figures/fig1_maps_cycle_idt.png)

*图 1：Maps 实验中 Cycle Consistency Loss（左）和 Identity Loss（右）随 epoch 的变化。灰色虚线标示 epoch 100（学习率衰减起点）。*

#### 3.1.3 GAN Loss（生成器 + 判别器）曲线

![Maps — GAN Loss](figures/fig2_maps_gan.png)

*图 2：Maps 实验中生成器损失 G_A/G_B（左）和判别器损失 D_A/D_B（右）随 epoch 的变化。*

#### 3.1.4 所有损失项汇总

![Maps — 所有损失项训练曲线](figures/fig7_maps_all_losses.png)

*图 3：Maps 实验全部 8 项损失的独立训练曲线，上排为方向 A（map→aerial），下排为方向 B（aerial→map）。*

**关键观察：**

1. **Cycle Consistency Loss 显著下降**：cycle_A 从初始的 ~2.0 下降至末期的 ~0.5，cycle_B 从 ~1.0 下降至 ~0.2，说明模型成功学习了双向映射的循环一致性。
2. **Identity Loss 快速收敛**：idt_A 从 ~0.5 下降至 ~0.07，idt_B 从 ~0.9 下降至 ~0.12，表明生成器能够很好地保持已属于目标域的输入。
3. **判别器损失稳定**：D_A 和 D_B 在训练过程中保持在 0.05~0.30 范围内波动，没有出现坍缩或爆炸现象，表明生成器和判别器之间维持了良好的对抗平衡。
4. **学习率衰减效果**：epoch 100 之后进入线性衰减阶段，损失继续缓慢下降，训练稳定收敛。

### 3.2 Monet2Photo 数据集训练损失

训练从 2026-03-11 02:07 开始，共完成 200 个 epoch。每个 epoch 约 6287 次迭代（Monet2Photo 数据集规模更大）。

#### 3.2.1 损失变化趋势

| 阶段 | Epoch | cycle_A | cycle_B | D_A | D_B | G_A | G_B | idt_A | idt_B |
|------|-------|---------|---------|-----|-----|-----|-----|-------|-------|
| 初期 | 1 | 0.4~4.3 | 0.4~5.0 | 0.03~0.58 | 0.03~0.57 | 0.06~1.12 | 0.05~1.25 | 0.11~2.50 | 0.16~2.19 |
| 中期 | ~100 | 0.4~0.9 | 0.4~1.4 | 0.05~0.33 | 0.03~0.32 | 0.2~0.9 | 0.2~1.0 | 0.11~0.55 | 0.16~0.38 |
| 末期 | 200 | 0.3~0.9 | 0.3~0.9 | 0.04~0.34 | 0.03~0.39 | 0.3~0.8 | 0.3~1.2 | 0.08~0.34 | 0.09~0.32 |

#### 3.2.2 Cycle Consistency Loss 与 Identity Loss 曲线

![Monet2Photo — Cycle Consistency Loss 与 Identity Loss](figures/fig3_monet_cycle_idt.png)

*图 4：Monet2Photo 实验中 Cycle Consistency Loss（左）和 Identity Loss（右）随 epoch 的变化。*

#### 3.2.3 GAN Loss（生成器 + 判别器）曲线

![Monet2Photo — GAN Loss](figures/fig4_monet_gan.png)

*图 5：Monet2Photo 实验中生成器损失（左）和判别器损失（右）随 epoch 的变化。*

#### 3.2.4 所有损失项汇总

![Monet2Photo — 所有损失项训练曲线](figures/fig8_monet_all_losses.png)

*图 6：Monet2Photo 实验全部 8 项损失的独立训练曲线。*

**关键观察：**

1. **初始损失高于 Maps 实验**：Monet2Photo 涉及照片与画作之间更大的风格差异，初始 cycle loss 可达 4~5，高于 Maps 实验的 2~3。
2. **收敛速度略慢但趋势一致**：由于任务复杂度更高，Monet2Photo 的损失下降更为平缓，但最终同样达到了良好的收敛状态。
3. **训练稳定性良好**：Monet 日志包含三段 session（首次训练、一次误启动、正式续训），经清洗后 epoch 108 处的续训衔接平滑，损失曲线无明显跳变，说明模型状态从断点正确恢复。需注意该结论基于清洗后的数据（丢弃了误启动 session 的 21 条记录），原始日志未经处理时 epoch 1 和 108 的数据会被多段 session 污染。

---

## 4. 复现结果与论文对比分析

### 4.1 训练行为一致性

| 指标 | 论文描述 | 复现观察 | 一致性 |
|------|----------|----------|--------|
| Cycle loss 收敛 | 重建图像与输入接近 (Figure 4) | cycle_A/B 从 ~2.0 降至 ~0.5 | ✅ |
| GAN 训练稳定 | LSGAN 提供稳定训练 | D_A/D_B 无坍缩/爆炸 | ✅ |
| 学习率策略 | 前100 epoch不变，后100线性衰减 | 损失在 epoch 100 后继续平稳下降 | ✅ |
| 图像缓冲池 | 50张历史图像减少模型振荡 | 判别器损失波动平稳 | ✅ |
| Identity loss | 帮助保持颜色一致性 | idt_A/B 快速收敛至低值 | ✅ |

### 4.2 训练可视化输出

两个实验均生成了完整的 epoch 级可视化图像（每个实验 1600 张，200 epoch × 8 张/epoch），包含：
- `real_A` / `real_B`：输入图像
- `fake_A` / `fake_B`：生成图像
- `rec_A` / `rec_B`：重建图像（循环一致性的直观验证）
- `idt_A` / `idt_B`：身份映射图像

### 4.3 模型保存完整性

两个实验均按照每 5 个 epoch 保存一次检查点，从 epoch 5 到 epoch 200，共 40 个检查点 + latest 检查点，每个检查点包含 4 个模型文件（G_A, G_B, D_A, D_B），共计：
- Maps: 41 × 4 = 164 个模型文件 ✅
- Monet: 41 × 4 = 164 个模型文件 ✅

### 4.4 测试结果分析

使用训练完成的模型（epoch 200 / latest）在测试集上进行推理，三个数据集均生成了完整的测试输出。

#### 4.4.1 测试输出概况

| 数据集 | 测试样本数 | 图片总数 | 每样本输出 |
|--------|-----------|---------|-----------|
| Maps (地图↔航拍) | 50 | 300 | real_A, fake_B, rec_A, real_B, fake_A, rec_B |
| Monet2Photo (莫奈↔照片) | 63 | 378 | real_A, fake_B, rec_A, real_B, fake_A, rec_B |
| Cityscapes (照片↔语义分割) | 50 | 300 | real_A, fake_B, rec_A, real_B, fake_A, rec_B |

其中每个样本包含 6 张图像，覆盖双向翻译的完整流程：
- **A→B 方向**：real_A（输入）→ fake_B（生成）→ rec_A（循环重建）
- **B→A 方向**：real_B（输入）→ fake_A（生成）→ rec_B（循环重建）

#### 4.4.2 Maps 测试结果定性分析

**A→B（航拍图→地图）**：模型能够从航拍图像中提取道路、水域、建筑的结构信息，生成具有合理颜色编码的地图。道路和主干道的识别较为准确，主要道路呈现为橙色/黄色标注，水域区域被正确识别为蓝色。重建图像（rec_A）与原始航拍图高度相似，验证了循环一致性约束的有效性。

**B→A（地图→航拍图）**：模型根据地图的结构信息生成逼真的航拍纹理，建筑区域呈现为密集灰色纹理，空地和水域的纹理区分明确。

#### 4.4.3 Monet2Photo 测试结果定性分析

**A→B（莫奈画作→真实照片）**：模型将莫奈特有的印象派笔触风格转换为写实照片风格，保留了场景的整体构图和色彩基调。天空从画作中的色块变为更自然的渐变，草地和植被的纹理更加写实。重建图像（rec_A）保持了原画的构图和色调，循环一致性良好。

**B→A（真实照片→莫奈风格）**：模型成功学习了莫奈的风格特征：柔和的笔触、明亮的色调、以及印象派特有的光影处理。风格迁移后的图像在保持原始场景结构的同时呈现出明显的油画质感。

#### 4.4.4 Cityscapes 测试结果定性分析

**A→B（真实照片→语义分割图）**：模型在无配对监督的条件下学习了从照片到语义分割的映射。道路（紫色）、人行道（粉色）、植被（绿色）、天空（蓝色）等主要语义区域的分割基本合理。但与有监督方法（如 pix2pix）相比，边界精度有限，小目标（如行人、交通标志）的分割不够精确，这与论文中 CycleGAN 在 Cityscapes 上的 FCN-score 低于 pix2pix 的结论一致。

**B→A（语义分割图→真实照片）**：模型根据语义区域生成具有合理纹理的照片，道路、建筑和植被的纹理分布与语义标注对应。但生成的照片整体偏灰暗，细节清晰度低于 Maps 和 Monet 实验，反映了 Cityscapes 任务中语义到纹理映射的模糊性更大。

> **注：** Cityscapes 使用的是预训练模型而非本次从零训练的模型，测试结果用于展示 CycleGAN 在不同任务上的泛化能力。

---

## 5. 两个数据集的对比分析

| 维度 | Maps (地图↔航拍) | Monet2Photo (莫奈↔照片) |
|------|-------------------|------------------------|
| 数据集规模 | ~1096 张/epoch | ~6287 张/epoch |
| 任务复杂度 | 结构化映射（颜色/纹理差异为主） | 风格迁移（笔触/色调/抽象化） |
| 初始 cycle_A | ~2.0 | ~2.5 |
| 最终 cycle_A | ~0.5 | ~0.5 |
| 初始 cycle_B | ~1.0 | ~2.5 |
| 最终 cycle_B | ~0.2 | ~0.5 |
| 收敛速度 | 较快（~50 epoch 基本稳定） | 较慢（~80 epoch 基本稳定） |
| 最终 idt_A | ~0.07 | ~0.15 |
| 最终 idt_B | ~0.12 | ~0.15 |
| 训练耗时/epoch | ~0.16 s/iter | ~0.11 s/iter |

#### 5.1 Cycle Loss 跨数据集对比

![两个数据集 Cycle Loss 对比](figures/fig5_cycle_compare.png)

*图 7：Maps 与 Monet2Photo 的 cycle_A（左）和 cycle_B（右）训练曲线对比。*

#### 5.2 Epoch 200 各损失项最终均值对比

![Epoch 200 各损失项最终均值对比](figures/fig6_final_loss_compare.png)

*图 8：两个实验在 epoch 200 时各损失项的平均值柱状对比图，柱顶标注具体数值。*

**分析：**

- Maps 的 cycle_B（航拍→地图→航拍重建）收敛到更低的值（0.206 vs 0.504），因为地图的结构化特征更容易精确重建。
- Monet2Photo 两个方向的 cycle loss 更为对称（0.511 vs 0.504），反映了照片和画作之间的信息复杂度更为均衡。
- Maps 的判别器 D_A 损失更低（0.085 vs 0.143），说明地图域的判别器更容易训练。
- Monet2Photo 的 G_B 损失明显更高（0.733 vs 0.313），反映了从照片生成莫奈风格画作的任务难度更大。

---

## 6. 训练稳定性评估

### 6.1 判别器-生成器平衡

GAN 训练的核心挑战是判别器和生成器之间的平衡。在两个实验中：

- **D_A/D_B 始终在 0.03~0.35 范围**：未出现判别器过强（D→0，G 无法学习）或过弱（D→0.5，无有效梯度）的情况。
- **G_A/G_B 波动在合理范围**：生成器损失在 0.1~1.0 之间波动，反映了 LSGAN 的稳定特性。

### 6.2 无模式坍缩迹象

- 如果发生模式坍缩，cycle loss 会突然升高且无法恢复。实验中 cycle loss 持续下降且无异常跳变，排除了模式坍缩。
- 论文中提到的图像缓冲池（pool_size=50）策略在复现中得到正确实施，对稳定训练起到了关键作用。

---

## 7. 结论

### 7.1 复现成功度

本次复现在以下方面与原论文保持了**完全一致**：

1. **网络架构**：ResNet-9blocks 生成器 + 3层 PatchGAN 判别器 + Instance Norm
2. **超参数设置**：λ=10, lr=0.0002, β1=0.5, batch_size=1, pool_size=50
3. **训练策略**：200 epoch，前100保持lr，后100线性衰减
4. **损失函数**：LSGAN + Cycle Consistency Loss + Identity Loss

### 7.2 训练结果总结

| 评估维度 | 结论 |
|----------|------|
| 损失收敛性 | ✅ 所有损失项均正常收敛 |
| 训练稳定性 | ✅ 无模式坍缩、梯度爆炸或训练振荡 |
| 循环一致性 | ✅ cycle loss 显著下降，重建质量良好 |
| 身份保持 | ✅ identity loss 快速收敛至低值 |
| 判别器平衡 | ✅ D 与 G 维持良好对抗均衡 |
| 参数一致性 | ✅ 与论文设定完全一致 |

### 7.3 测试结果总结

| 数据集 | A→B 生成质量 | B→A 生成质量 | 循环重建质量 |
|--------|-------------|-------------|-------------|
| Maps | ✅ 道路/水域/建筑结构正确映射 | ✅ 纹理真实，区域区分明确 | ✅ 与输入高度一致 |
| Monet2Photo | ✅ 笔触→写实转换自然 | ✅ 成功学习莫奈风格特征 | ✅ 保持构图和色调 |
| Cityscapes | ⚠️ 主要语义区域合理，细节有限 | ⚠️ 纹理合理但偏灰暗 | ✅ 基本一致 |

Cityscapes 的表现与论文一致：CycleGAN 在无配对监督下的分割精度低于 pix2pix 等有监督方法（论文 Table 2/3），但在 Maps 和 Monet 等风格/纹理迁移任务上表现出色。

### 7.4 后续建议

1. **定量评估**：论文使用 FCN-score（分割网络评估生成质量）和 AMT 人工感知测试进行量化评估。建议计算 FID（Frechet Inception Distance）等指标，进一步量化生成图像质量。
2. **消融实验**：论文 Table 4/5 展示了去除 GAN loss 或 cycle loss 的消融实验结果，可考虑复现以验证各损失项的贡献。

---

*报告生成日期：2026-03-15*
*实验平台：macOS Darwin 25.2.0*
*训练框架：PyTorch (CycleGAN official implementation)*
